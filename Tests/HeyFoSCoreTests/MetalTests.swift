// Tests temporarily disabled - will implement with XCTest or manual test harness
// Run `swift run heyfos-cli --verbose` to verify Metal initialization

/*
@testable import HeyFoSCore

struct MetalTests {
    static func run() throws {
        print("Testing Metal initialization...")
        let context = try MetalContext()
        assert(context.device != nil)
        assert(context.laplacianPipeline != nil)
        print("✓ All Metal tests passed")
    }
}
*/
